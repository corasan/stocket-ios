name: CI

on:
  pull_request:
    branches:
      - master
      - develop

jobs:
  build:

    runs-on: macos-latest
    
    strategy:
      matrix:
        destination: ['platform=iOS Simulator,OS=13.1,name=iPhone 11 Pro', 'platform=iOS Simulator,OS=13.1,name=iPhone 11']

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build
        run: xcodebuild clean test -project StocketiOS.xcodeproj -scheme Development -destination "${destination}" CODE_SIGN_IDENTITY="" CODE_SIGNING_REQUIRED=NO ONLY_ACTIVE_ARCH=NO
